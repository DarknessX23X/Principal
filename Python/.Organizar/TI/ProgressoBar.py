from tqdm import tqdm
import time


# Crie um iterável (por exemplo, uma lista)
iterable = range(100)

# Use a função tqdm para criar a barra de progresso
for item in tqdm(iterable):
    # Faça algo aqui (por exemplo, processamento ou espera)
    time.sleep(0.5)




